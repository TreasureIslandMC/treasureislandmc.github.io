import React, { JSX } from 'react';
import Layout from '@theme/Layout';

export default function ToolsPage(): JSX.Element {
    return (
        <Layout
            title="Tools"
            description="Tools for TreasureIslandMC"
        >
            <div style={{
                padding: '2rem',
                textAlign: 'center',
            }}>
                {/* Empty for now - add content later */}
            </div>
        </Layout>
    );
}